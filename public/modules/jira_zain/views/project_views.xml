<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============ PROJECT FORM (Rich with stat buttons) ============ -->
    <record id="view_project_form" model="ir.ui.view">
        <field name="name">jira_zain.project.form</field>
        <field name="model">jira_zain.project</field>
        <field name="arch" type="xml">
            <form string="Project">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" type="action"
                                name="%(jira_zain.action_task)d" icon="fa-tasks"
                                context="{'default_project_id': active_id, 'search_default_project_id': active_id}">
                            <field string="Tasks" name="task_count" widget="statinfo"/>
                        </button>
                        <button class="oe_stat_button" type="action"
                                name="%(jira_zain.action_sprint)d" icon="fa-rocket"
                                context="{'default_project_id': active_id, 'search_default_project_id': active_id}">
                            <field string="Sprints" name="sprint_count" widget="statinfo"/>
                        </button>
                        <button class="oe_stat_button" icon="fa-bug" disabled="1">
                            <field string="Bugs" name="bug_count" widget="statinfo"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Archived" bg_color="bg-danger"
                            attrs="{'invisible': [('active', '=', True)]}"/>
                    <div class="oe_title">
                        <label for="name" string="Project Name"/>
                        <h1>
                            <field name="name" placeholder="Project Name..."/>
                        </h1>
                    </div>
                    <group>
                        <group string="Project Details">
                            <field name="code"/>
                            <field name="manager_id" widget="many2one_avatar_user"/>
                            <field name="date_start"/>
                            <field name="date_end"/>
                            <field name="active" invisible="1"/>
                        </group>
                        <group string="Progress">
                            <field name="progress" widget="progressbar"/>
                            <field name="member_ids" widget="many2many_tags"
                                   options="{'color_field': 'color'}"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <field name="description" placeholder="Project description..."/>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <!-- ============ PROJECT LIST (Rich with progress bar) ============ -->
    <record id="view_project_tree" model="ir.ui.view">
        <field name="name">jira_zain.project.tree</field>
        <field name="model">jira_zain.project</field>
        <field name="arch" type="xml">
            <tree string="Projects">
                <field name="sequence" widget="handle"/>
                <field name="code" string="Key"/>
                <field name="name" string="Project Name"/>
                <field name="manager_id" string="Manager" widget="many2one_avatar_user"/>
                <field name="task_count" string="Tasks"/>
                <field name="bug_count" string="Bugs"/>
                <field name="sprint_count" string="Sprints"/>
                <field name="progress" string="Progress" widget="progressbar"/>
                <field name="date_start"/>
                <field name="date_end"/>
            </tree>
        </field>
    </record>

    <!-- ============ PROJECT KANBAN ============ -->
    <record id="view_project_kanban" model="ir.ui.view">
        <field name="name">jira_zain.project.kanban</field>
        <field name="model">jira_zain.project</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <field name="color"/>
                <field name="progress"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="oe_kanban_color_#{kanban_getcolor(record.color.raw_value)} oe_kanban_card oe_kanban_global_click">
                            <div class="oe_kanban_content">
                                <div class="o_kanban_record_top">
                                    <div class="o_kanban_record_headings">
                                        <strong>
                                            <span class="badge text-bg-primary me-2"><field name="code"/></span>
                                            <field name="name"/>
                                        </strong>
                                    </div>
                                </div>
                                <div class="o_kanban_record_body mt-2">
                                    <field name="progress" widget="progressbar"/>
                                </div>
                                <div class="o_kanban_record_bottom mt-2">
                                    <div class="oe_kanban_bottom_left">
                                        <span class="me-2"><i class="fa fa-tasks"/> <field name="task_count"/></span>
                                        <span><i class="fa fa-bug"/> <field name="bug_count"/></span>
                                    </div>
                                    <div class="oe_kanban_bottom_right">
                                        <field name="manager_id" widget="many2one_avatar_user"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Project Search -->
    <record id="view_project_search" model="ir.ui.view">
        <field name="name">jira_zain.project.search</field>
        <field name="model">jira_zain.project</field>
        <field name="arch" type="xml">
            <search string="Search Projects">
                <field name="name"/>
                <field name="code"/>
                <field name="manager_id"/>
                <filter string="My Projects" name="my_projects"
                        domain="['|', ('manager_id', '=', uid), ('member_ids', 'in', uid)]"/>
                <filter string="Archived" name="archived" domain="[('active', '=', False)]"/>
                <group expand="0" string="Group By">
                    <filter string="Manager" name="group_manager" context="{'group_by': 'manager_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Project Action -->
    <record id="action_project" model="ir.actions.act_window">
        <field name="name">Projects</field>
        <field name="res_model">jira_zain.project</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="search_view_id" ref="view_project_search"/>
        <field name="context">{'search_default_my_projects': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first project
            </p>
        </field>
    </record>

</odoo>
